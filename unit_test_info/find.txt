Creates a valid tree, sets student's root pointer to the root of the valid tree, and checks the result of student-implemented .find(j) function for all j in some arbitrary sequence.

If the student-implemented function returns the wrong result for any j that was tested, the unit test is failed.